server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/brtacer/gitconfig.git"
          default-label: main
          skip-ssl-validation: true
          username: brtacer
          password: ${GIT_CONFIG}